import React, { ReactElement } from 'react'

interface Props {
    
}

function Donations({}: Props): ReactElement {
    return (
        <div>
            Donations
        </div>
    )
}

export default Donations
