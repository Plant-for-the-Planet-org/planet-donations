import React, { ReactElement } from 'react'

interface Props {
    
}

function Footer({}: Props): ReactElement {
    return (
        <div>
            Footer
        </div>
    )
}

export default Footer
