import React, { ReactElement } from 'react'

interface Props {
    
}

function index({}: Props): ReactElement {
    return (
        <div>
            Home
        </div>
    )
}

export default index
